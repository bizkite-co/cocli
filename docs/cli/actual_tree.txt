cocli
    --campaign/-c (text)
    --help-text/-ht (boolean)
    add - Add a new company or a person to an existing company.
        --company/-c (text) [required]
        --person/-p (text)
    add-email - Adds an email address to a company and records it in the campaign index.
        company_name (text) [required]
        email (text) [required]
        --campaign/-c (text)
    add-meeting - Add a new meeting for a company.
        --company/-c (text) [required]
        --date/-d (text)
        --time/-t (text)
        --title/-T (text)
    admin - Administrative commands for system management.
        archive-campaign - Creates a compressed archive of a campaign and uploads it to S3.
            campaign_name (text) [required]
            --upload (boolean)
            --keep-local (boolean)
        refresh-dev - Refreshes the local DEV environment data from PROD.
            --force/-f (boolean)
            --s3 (boolean)
    audit - Auditing tools for the cocli system structure and integrity.
        campaign - Whole-campaign leak audit: trace every place_id gm-list has ever
            --campaign/-c (text)
            --limit (integer)
            --out (path)
        cli - Dumps the CLI command hierarchy.
            --output/-o (path)
        cluster - Audit cluster nodes: designation (content_type/workers), queue depths, and a
            --campaign/-c (text)
            --verbose/-v (boolean)
            --s3/--fast (boolean)
        enrichment - Audit website enrichment metrics for a campaign: counts of enriched companies,
            --campaign/-c (text)
        enrichment-interactive - Interactively step through companies that have no contact name (Tiers 2 & 3)
            --campaign/-c (text)
        fs - Audits the filesystem for OMAP compliance and Screaming Architecture.
            --campaign/-c (text)
            --output/-o (path)
            --gen-cleanup (boolean)
            --skip-companies (boolean)
        gm-list-html - Audit gm-list HTML files to extract and verify rating/review data.
            --campaign/-c (text)
            --limit/-n (integer)
            --output/-o (text)
        queue - Audit specific queues.
            export-cases - Export field-level corrections as parametrized test cases.
                campaign (text)
                --tile (text)
                --phrase/-p (text)
            gm-list - Run full end-to-end audit for the gm-list queue (compile, compact, report).
                --campaign/-c (text)
            mission-reconciliation - Reconcile gm-list's mission (discovery-gen/completed), pending
                --campaign/-c (text)
                --verbose/-v (boolean)
                --sample-limit (integer)
            purge-leases - Purge stale or expired leases from a queue directory.
                --campaign (text)
                --queue-name (text)
                --force (boolean)
                --dry-run (boolean)
                --max-age-minutes (integer)
            replay - Replay audit corrections onto a USV results file.
                campaign (text)
                usv_path (path) [required]
                --output/-o (path)
                --corrections (path)
                --reviewed (path)
            tile-status - Audit tile-queue status: pending/completed tile counts.
                --campaign (text)
            validate - Human-in-the-loop validation for gm-list results.
                campaign (text)
                --tile (text)
                --phrase/-p (text)
                --usv-path (path)
                --headed (boolean)
                --limit/-n (integer)
        rollout - Automated diagnostic for active rollout batches across the cluster.
            --campaign/-c (text)
            --name/-n (text)
            --cluster (boolean)
        schemas - Audit datapackage.json files for schema compliance.
            --campaign/-c (text)
            --fix (boolean)
            --dry-run (boolean)
        scrape - Audit the whole scrape workflow for a campaign.
            --campaign/-c (text)
            --verbose/-v (boolean)
            --threshold (integer)
            --output/-o (path)
            --no-duckdb (boolean)
            --summary-only (boolean)
        tui - Dumps the TUI widget hierarchy.
            --output/-o (path)
        tui-actions - Dumps every action (keybinding or command-palette-only) exposed by the
            --output/-o (path)
    campaign - Manage campaigns.
        achieve-goal - Deprecated - superseded by Discovery Gen; not called in production since ~2026-06.
            --emails/--limit (integer)
            campaign_name (text)
            --force/-f (boolean)
            --ttl-days (integer)
            --proximity (float)
            --grid (boolean)
            --resource-discovery (boolean)
        add - Adds a new campaign.
            name (text) [required]
            company (text) [required]
        add-location - Adds a target location to the campaign.
            location (text) [required]
            --campaign/-c (text)
        add-query - Adds a search query to the campaign configuration.
            query (text) [required]
            --campaign/-c (text)
        audit - Campaign audit and data exploration
            companies - List companies found in a specific tile.
                campaign_name (text)
                --tile-id (text)
            company - Show detailed information for a company.
                campaign_name (text)
                --place-id (text)
            locations - List all target locations for a campaign.
                campaign_name (text)
            summary - Show overall campaign audit summary.
                campaign_name (text)
                --cluster (boolean)
            tiles - List tiles for a specific location.
                campaign_name (text)
                --location-index (integer)
        bucket - Displays the S3 bucket root and campaign path.
            --campaign/-c (text)
        build-mission-index - Explodes a mission list into the Active Task Pool (discovery-gen/completed/).
            campaign_name (text)
            --batch (text)
        compile-lifecycle - Compiles the lifecycle index from local completed queues.
            campaign_name (text)
        compile-to-call - Compiles prospects to a To-Call list. "Add more" (no --purge) is the
            campaign_name (text)
            --limit (integer)
            --purge (boolean)
            --dry-run (boolean)
        coverage-gap - Generates a report matching every target area to its most recent scrape date.
            campaign_name (text)
            --output (path)
        create-batch - Creates a named batch subset from the pending frontier for controlled rollout.
            campaign_name (text)
            --name (text)
            --limit (integer)
            --offset (integer)
            --query (text)
        curate-events - Interactive CLI tool to review and curate upcoming events.
            campaign_name (text)
        discover-venues - Discovers local community venues and institutions via Google Maps.
            campaign_name (text)
            --limit/-l (integer)
            --force/-f (boolean)
            --headed (boolean)
        edit - Edits an existing campaign's configuration.
            campaign_name (text)
        events - Campaign events and discovery
            generate-tasks - Hydrates EventSource templates into time-specific EventScrapeTasks.
                --campaign (text)
                --windows (integer)
        exclude - Manage campaign-specific exclusions
            add - Excludes a company (by slug or domain) from a campaign, or globally
                target (text) [required]
                --campaign/-c (text)
                --reason/-r (text)
                --scope (text)
            list - Lists exclusions for a campaign, or the shared global list (--scope global).
                --campaign/-c (text)
                --scope (text)
            remove - Removes an exclusion from a campaign, or from the global list (--scope global).
                target (text) [required]
                --campaign/-c (text)
                --scope (text)
        export-resources - Aggregates and exports "Value-First" resources from the campaign index.
            campaign_name (text)
        generate-grid - Generates a 0.1-degree aligned scrape grid for each target location in the campaign.
            campaign_name (text)
            --proximity (float)
        geocode-locations - Scans the campaign's target locations CSV and fills in missing geocoordinates.
            campaign_name (text)
        import-contacts - Imports contacts from a CSV file into a campaign.
            csv_path (path) [required]
            campaign_name (text)
        import-prospects - Imports prospects from a campaign's prospects.csv into the canonical company structure.
            campaign_name (text)
        list - List local campaigns with descriptions.
            --paths (boolean)
        monitor-batch - Monitor the progress of a discovery batch.
            campaign_name (text)
            --name (text)
            --recent (boolean)
            --cluster (boolean)
        next-step - Advances the campaign workflow to the next logical step.
            campaign_name (text)
        path-check - Audits specific data paths across the local machine, PI cluster, and S3.
            paths (text) [required]
            --campaigns/-c (text)
        prepare-mission - Generates a deterministic master task list (mission.usv) and calculates the unscraped frontier.
            campaign_name (text)
            --ttl-days (integer)
            --debug (boolean)
        prospects - Manage prospects within a campaign
            enrich-from-queue - Consumes enrichment tasks from the campaign's queue.
                campaign_name (text)
                --runner (text)
                --batch-size (integer)
                --cloud-queue (boolean)
                --dual-purpose (boolean)
            tag-from-csv - Updates company tags based on the prospects.csv file for the current campaign.
            to-hubspot-csv - Exports campaign prospects to a HubSpot CSV file.
                --with-email (boolean)
                --city (text)
                --state (text)
        publish-kml - Generates all KMLs (Customers, Prospects, Coverage) and uploads them to S3.
            campaign_name (text)
            --bucket (text)
            --domain (text)
            --profile (text)
        queue-batch - Enqueues or exports a batch of scrape tasks for the campaign.
            campaign_name (text)
            --limit (integer)
            --force (boolean)
            --dry-run (boolean)
            --output/-o (path)
            --no-limit (boolean)
        queue-enrichment - Enqueues companies from the campaign for website enrichment into the DFQ.
            campaign_name (text)
            --force/-f (boolean)
            --limit/-l (integer)
            --has-email (boolean)
        queue-mission - Reports the status of the Discovery Generation queue.
            campaign_name (text)
        queue-scrapes
            campaign_name (text)
            --include-legacy (boolean)
            --force (boolean)
            --dry-run (boolean)
        remove-location - Removes a target location from the campaign.
            location (text) [required]
            --campaign/-c (text)
        remove-query - Removes a search query from the campaign configuration.
            query (text) [required]
            --campaign/-c (text)
        restore-names - Restores company names from the Google Maps index and writes provenance receipts.
            campaign_name (text)
            --dry-run (boolean)
        rollout - Standardized campaign rollout management
            audit - Pulls results from all cluster nodes and runs a quality audit.
                --campaign/-c (text)
            broadcast-config - Broadcasts the current scaling configuration to all cluster nodes via Gossip.
                --campaign/-c (text)
            progress - Show real-time progress of active rollout.
                campaign_name (text)
                --watch/-w (boolean)
            pull-config - Downloads campaign config.toml from S3 to local disk (campaigns/{campaign}/config.toml).
                --campaign/-c (text)
            push - Propagates local campaign discovery tasks and batches to the cluster.
                --campaign/-c (text)
                --delete (boolean)
            push-config - Uploads the local campaign config.toml to S3 (campaigns/{campaign}/config.toml).
                --campaign/-c (text)
            report - Detailed audit report: batches, coverage, and synced results.
                campaign_name (text)
            run - Executes a standardized discovery rollout:
                campaign_name (text)
                --name (text)
                --limit (integer)
                --ttl-days (integer)
                --force/-f (boolean)
                --purge (boolean)
                --monitor (boolean)
            status - Show current rollout status: batches deployed, and synced results.
                campaign_name (text)
            sync - Sync scraping results from PIs back to hub via S3.
                campaign_name (text)
                --deduplicate (boolean)
                --workers (integer)
                --full (boolean)
                --force (boolean)
        sanitize-discovery - High-Fidelity Discovery Reset: Pulls from S3, purges junk/hollow USVs, and propagates to PIs.
            campaign_name (text)
        set - Sets the current campaign context.
            campaign_name (text) [required]
        show - Displays the current campaign context.
        start-workflow - Starts the campaign workflow.
            campaign_name (text)
        status - Displays the current state of the campaign workflow.
            campaign_name (text)
        unset - Clears the current campaign context.
        upload-kml-coverage - Legacy command for manual placement into turboship repo.
            campaign_name (text)
            --turboship-kml-exports-path (path)
            --filename (text)
            --type (text)
        visualize-coverage - Generates KML files to visualize the scraped areas for a campaign.
            campaign_name (text)
        visualize-legacy-scrapes - Generates a KML file for all legacy (non-grid-aligned) scraped areas.
            campaign_name (text)
    cluster - Manage the Raspberry Pi worker cluster.
        deploy-hotfix - Safe Cluster Deployment: Builds on Hub (cocli5x1), pushes to registry, and spokes pull.
            --campaign/-c (text)
            --force (boolean)
        gossip-audit - Diagnostic tool for the cluster Gossip Bridge.
            --target/-t (text)
            --timeout (float)
        prune - Prunes unused Docker objects (containers, images, build cache) across all known cluster nodes.
        status - Checks health and container status of all nodes in the cluster.
            --campaign/-c (text)
        stop - Stops all cocli worker containers across all cluster nodes.
            --campaign/-c (text)
        sync-clocks - Synchronizes clocks across the cluster using the Hub as the authority.
            --source (text)
        top - Real-time performance monitor for the cluster (CPU, Temp, Workers).
            --campaign/-c (text)
    companies - Commands for managing companies.
        backfill-from-prospects - Materializes companies/<slug> directories for prospects that exist in a
            --campaign/-c (text) [required]
            --execute (boolean)
            --min-hours-since-last-run (float)
        list-recent - Lists the most recently created companies.
            --count/-c (integer)
    compile-enrichment - Compiles enrichment data from various sources into the main company _index.md files.
        --campaign/-c (text)
    context - Sets, shows, or clears the current context.
        filter_str (text)
    data - Utilities for interacting with frictionless data files.
        compact-emails - Compact the email index's hot inbox into cold shards (Hot Inbox -> Shards).
            --campaign/-c (text) [required]
        describe - Show the schema definition (fields/types) for a given USV file or datapackage.
            file_path (path) [required]
        export-enriched-emails - Joins the prospects checkpoint against the email index and writes the
            --campaign/-c (text) [required]
            --keywords (boolean)
            --all/-a (boolean)
        inspect - Show the index, name, and content for a specific row in a USV file.
            file_path (path) [required]
            --row-number/-r (integer)
        job-run - Scrape job run inspection commands
            list - List ScrapeJobRuns for a campaign - the explicit lineage between a
                --campaign/-c (text) [required]
                --limit (integer)
            requeue - Create a new ScrapeJobRun that re-scrapes a run's exact identity
                run_id (text)
                --campaign/-c (text) [required]
                --latest (boolean)
        list - List all known frictionless data schemas (datapackage.json files) in the project.
        locate - Find and display the datapackage.json for a given file.
            file_path (path) [required]
        metrics - Compute data quality metrics for a USV dataset (or datapackage).
            file_path (path) [required]
            --resource (text)
            --output/-o (path)
        queue - Queue management commands
            compact - Compact a queue's results into a unified dataset.
                --campaign/-c (text)
                queue_name (text) [required]
            enqueue-gm-list - Copy discovery-gen/completed/ -> gm-list/pending/.
                --campaign/-c (text) [required]
                --limit (integer)
                --rescrape-all (boolean)
                --dry-run (boolean)
            reconcile - Diff two directory trees of task/result files by normalized identity.
                left (path) [required]
                right (path) [required]
                --strip-leading-segments (integer)
                --verbose/-v (boolean)
                --sample-limit (integer)
        retrofit-personnel-names - Backfill person: tags onto already-indexed emails whose name wasn't
            --campaign/-c (text) [required]
            --apply (boolean)
        sample - Show first N rows with column names from a USV file.
            file_path (path) [required]
            --limit/-n (integer)
            --text (boolean)
            --resource (text)
        search - Provide a schema-aware search interface for USV files using DuckDB.
            file_path (path) [required]
            query (text) [required]
            --columns (text)
    deduplicate
        deduplicate - Deduplicates company data by generating a stable hash for each company and removing duplicates.
            --dry-run (boolean)
    dev
        process-map-tile - Process map-tile/pending/ → discovery-gen/completed/, then auto-enqueue
            campaign_name (text)
            --max/-m (integer)
            --dry-run (boolean)
        run-discovery-gen-stages - Execute the discovery-gen pipeline: locations → tiles → mission → map-tile.
            campaign_name (text)
            --stage/-s (integer)
            --skip-validation (boolean)
        run-discovery-pipeline - Run the discovery-gen pipeline end-to-end with incremental validation.
            campaign_name (text)
            --stage/-s (integer)
            --dry-run (boolean)
    do-not-call - Manage the shared, company-wide do-not-call list (keyed by phone number).
        add - Adds a phone number to the shared do-not-call list. Applies across all
            phone (text) [required]
            --reason/-r (text)
        list - Lists every phone number on the shared do-not-call list.
        remove - Removes a phone number from the shared do-not-call list.
            phone (text) [required]
    email - Send via SES and poll a configured IMAP inbox into company notes. Not a mail client.
        poll - Poll configured IMAP folders and write notes for messages that match a company.
            --limit (integer)
        send - Send one email via SES and record a note on the matching company.
            --to (text) [required]
            --subject (text) [required]
            --body (text) [required]
            --company (text)
            --from (text)
    enrich - Commands for enriching company data.
        contacts - Scrape contact information from a company's website and add/update them in the CRM.
            company_name (text) [required]
        list - List all available enrichment scripts.
            --data-dir/-d (path)
        run - Run a specific enrichment script on one or all companies.
            script_name (text)
            --company/-c (text)
            --all/-a (boolean)
            --unenriched-only/-u (boolean)
            --data-dir/-d (path)
    enrich-customers - Enrich existing customers for a campaign with Google Maps data.
        campaign_name (text) [required]
    enrich-shopify-data - Enriches company data from a list of Shopify domains.
        --input/-i (text)
        --force/-f (boolean)
        --headed (boolean)
        --devtools (boolean)
        --debug (boolean)
    exclude - Manage campaign and shared exclusions
        add - Excludes a company (by slug or domain) from a campaign, or globally
            target (text) [required]
            --campaign/-c (text)
            --reason/-r (text)
            --scope (text)
        list - Lists exclusions for a campaign, or the shared global list (--scope global).
            --campaign/-c (text)
            --scope (text)
        remove - Removes an exclusion from a campaign, or from the global list (--scope global).
            target (text) [required]
            --campaign/-c (text)
            --scope (text)
    flag-email-providers - Flags a list of domains as email providers in the website cache.
        domains (text) [required]
    fz - Fuzzy search for companies and people using fzf and open the selection.
        --filter/-f (text)
        --force-rebuild-cache (boolean)
    google-maps-cache-to-company-files - Imports prospects from a CSV file, creating or updating companies and their enrichments.
        prospects_csv_path (file)
        --tag (text)
        --campaign/-c (text)
        --companies-dir (path)
        --match-threshold (integer)
    google-maps-csv-to-google-maps-cache - Ingests a CSV file with Google Maps data into the Google Maps cache.
        csv_path (file)
        --campaign/-c (text)
    help - Search every cocli command and subcommand's name and description for a
        phrase (text) [required]
        --limit/-n (integer)
    import-customers - Imports customers and their addresses from CSV files, creating companies and people.
        customers_csv_path (file) [required]
        addresses_csv_path (file) [required]
        --tag (text) [required]
        --campaign/-c (text)
    import-data - Import data using a specified importer.
        importer_name (text) [required]
        file_path (path)
        --debug (boolean)
    index - Commands for managing sharded indexes.
        archive-incomplete-wal - Move WAL records that don't match the current full schema (fewer
            --campaign/-c (text) [required]
            --index (text)
            --required-fields (integer)
            --apply (boolean)
        backfill-domains - Backfill the Domain Index from local website enrichment files.
            --campaign (text)
            --limit/-l (integer)
            --compact (boolean)
        clean-quote-corruption - Strips literal double-quote characters from checkpoint text fields
            --campaign (text)
            --index (text)
            --apply (boolean)
        compact - Compact the Write-Ahead Log (WAL) into the main Checkpoint.
            --campaign (text)
            --index (text)
            --debug (boolean)
        purge-invalid-place-ids - Removes checkpoint rows whose place_id is in a legacy Google CID
            --campaign (text)
            --index (text)
            --apply (boolean)
        requeue-enrichment-gaps - Recover the "Identity Gap (enrichment-enqueue)" gap `cocli audit
            place_id (text)
            --from-file (path)
            --from-audit-csv (path)
            --campaign (text)
        requeue-missing-details - Push a fresh gm-details task for place_ids the checkpoint already knows
            place_id (text)
            --from-file (path)
            --campaign (text)
            --batch-size (integer)
        requeue-stuck-details - Recover gm-details tasks that were acked with no real output - the
            place_id (text)
            --from-file (path)
            --campaign (text)
            --index (text)
        status - Show the status of the index tiers (WAL, Processing, Checkpoint).
            --campaign (text)
            --index (text)
        sync-pi-wal - Pushes each Pi node's local index WAL to S3 (rsync Pi -> local staging,
            --campaign (text)
            --index (text)
        trace - Trace one or more place_ids across every station of the pipeline
            place_id (text)
            --from-file (path)
            --campaign (text)
            --index (text)
            --out (path)
        write-datapackage - Generates Frictionless Data 'datapackage.json' for the specified index based on its Pydantic model.
            index (text) [required]
            --campaign/-c (text)
            --force/-f (boolean)
    infrastructure - Manage cocli infrastructure (Raspberry Pis, AWS).
        deploy-infra - Deploys AWS Infrastructure using CDK.
            --campaign/-c (text)
        start-worker - Starts a cocli worker role on a specific host.
            host (text) [required]
            --role (text)
            --count/-n (integer)
            --campaign/-c (text)
            --user/-u (text)
        stop-workers - Stops all cocli workers on a specific host.
            host (text) [required]
            --user/-u (text)
    init - Initializes the cocli configuration file.
    next - Lists upcoming meetings and allows interactive selection.
    open-company-folder - Open the company's folder in nvim.
        company_name (text) [required]
    process-shopify-scrapes - Compiles and deduplicates scraped Shopify data from multiple CSV files into a single index file.
        --output/-o (text)
    query - Commands for querying company data.
        query-prospects-location - Find prospects within a certain radius of a city, using the prospects CSV for speed.
            city (text) [required]
            --radius/-r (float)
            --campaign/-c (text)
    recent - Lists recent meetings and allows interactive selection.
    render
        kml - Render a KML file for a campaign.
            campaign_name (text) [required]
            --output-dir/-o (path)
        kml-coverage - Render a KML file for the 'turboship' campaign and place it in the turboship project's
            campaign_name (text)
            --turboship-kml-exports-path (path)
    render-prospects-kml - Generates a KML file for prospects from a CSV file for a specific campaign,
        campaign_name (text) [required]
    scrape-shopify-myip - Scrapes Shopify store information from myip.ms.
        --ip-index/-i (integer)
        --start-page/-s (integer)
        --end-page/-e (integer)
        --output-dir/-o (path)
        --debug (boolean)
    smart-sync
        active-leases
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        all - Syncs everything for a campaign (Config, Prospects, Emails, and Queues).
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        campaign-config
            --campaign/-c (text)
        companies
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        emails
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        enrichment-queue
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        prospects
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        queues
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        raw - Syncs raw HTML witnesses (both details and list items) from S3.
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        scraped-areas
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
        scraped-tiles
            --campaign/-c (text)
            --workers (integer)
            --full (boolean)
            --force (boolean)
    stations - Stations substrate tools (read-only inspect via the stations package).
        inspect - Render a campaign station root via stations inspect (read-only).
            queue (text)
            --path (path)
            --plain (boolean)
            --no-leases (boolean)
    status - Displays the current status of the cocli environment and campaign metrics.
        --campaign (text)
        --refresh/-r (boolean)
        --local/-l (boolean)
    sync - Commands for syncing data with S3.
        config - Syncs the campaign config.toml file with S3.
            campaign_name (text) [required]
            --direction (text)
            --dry-run (boolean)
        indexes - Bidirectional sync of indexes with S3.
            campaign_name (text)
            --dry-run (boolean)
        pi-results - Sync gm-list results from Pi workers.
            --campaign/-c (text)
            --force/-f (boolean)
            --status/-s (boolean)
        queue - Syncs a specific campaign queue with S3.
            campaign_name (text) [required]
            queue_name (text) [required]
            --direction (text)
            --delete (boolean)
            --limit/-l (integer)
            --dry-run (boolean)
    task - Manage development tasks and architectural issues.
        create - Create a new task in the mission queue.
            title (text) [required]
            --slug/-s (text)
            --body/-b (text)
            --draft (boolean)
            --depends-on/-d (text)
        done - Move a task to COMPLETED and create a Git commit.
            slug (text)
            --message/-m (text)
            --body/-b (text)
        list - List all tasks from the mission index.
        next - Show the current objective from the top of the mission index.
        prioritize - Update the ordinal position of a task in the mission index.
            slug (text) [required]
            position (integer) [required]
        start - Move a task to ACTIVE. Defaults to the first PENDING task.
            slug (text)
        sync - Sync the task index with the filesystem (discovering active/pending/draft).
        tree - Show a visual dependency tree of tasks.
    tui - Launches the Textual TUI for cocli.
        --dump-tree (path)
    video
        add - Add (import) an external video into the campaign raw queue.
            -c/--campaign (text)
            video (text) [required]
            --normalize (boolean)
            --profile (text)
        auth - Authenticate with YouTube using OAuth Device Code Flow.
            -c/--campaign (text)
        chapters - Regenerate chapters.md from existing transcripts (no Whisper / no encode).
            -c/--campaign (text)
            video_slug (text) [required]
            --provider (text)
            --also-packaged (boolean)
        create-thumbnail - Create a thumbnail for a video using metadata.
            -c/--campaign (text)
            video_slug (text) [required]
            --force/-f (boolean)
        extract-screenshots - Extract candidate screenshots from a video.
            -c/--campaign (text)
            video (text) [required]
        import - Add (import) an external video into the campaign raw queue.
            -c/--campaign (text)
            video (text) [required]
            --normalize (boolean)
            --profile (text)
        normalize - Normalize + transcribe videos in the raw queue (encode, STT, chapters, captions).
            -c/--campaign (text)
            --profile (text)
        package - Package videos from the normalized queue.
            -c/--campaign (text)
            --force/-f (boolean)
        transcribe - Run STT (+ chapters + captions) on an already-normalized video.
            -c/--campaign (text)
            video_slug (text) [required]
            --force/-f (boolean)
        upload - Upload a video to YouTube using metadata from the packaged queue.
            -c/--campaign (text)
            -v/--video (text)
            -p/--privacy (text)
            --force-privacy (boolean)
            --dry-run (boolean)
    view-company - View details of a specific company.
        company_slug (text) [required]
    view-meetings - View all meetings for a specific company.
        company_name (text) [required]
    web - Manage web deployment.
        deploy - Deploys the web dashboard (shell) and campaign data (KMLs, Report) to S3.
            --campaign-name/--campaign (text)
            --profile (text)
            --bucket (text)
            --domain (text)
        export-emails - Refreshes and uploads just the customer-facing enriched-emails CSV/USV -
            --campaign-name/--campaign (text)
            --profile (text)
            --bucket (text)
        report - Generates a JSON report for the campaign.
            --campaign-name/--campaign (text)
            --output (path)
    worker
        enrichment - Starts a worker node that polls for enrichment tasks (Domains) and scrapes them.
            --campaign/-c (text)
            --headed (boolean)
            --debug (boolean)
            --workers/-w (integer)
            --role (text)
        gm-details - Starts a worker node that polls for Google Maps Details tasks (Place IDs) and scrapes them.
            --campaign/-c (text)
            --headed (boolean)
            --debug (boolean)
            --workers/-w (integer)
            --role (text)
        gm-list - Starts a worker node that polls for Google Maps List tasks and executes them.
            --campaign/-c (text)
            --headed (boolean)
            --debug (boolean)
            --workers/-w (integer)
            --role (text)
        gossip - Starts the Gossip Bridge to propagate WAL datagrams across the cluster.
            --debug (boolean)
        orchestrate - Starts orchestrated workers for this node as defined in the campaign cluster config.
            --campaign/-c (text)
            --headed (boolean)
            --debug (boolean)
        supervisor - Starts a supervisor that dynamically manages scrape and details workers based on config.
            --campaign/-c (text)
            --headed (boolean)
            --debug (boolean)
            --interval (integer)
