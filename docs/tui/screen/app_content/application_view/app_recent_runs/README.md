# App Recent Runs

Right-hand column in Application View showing history of operations.

## Structure

```text
Vertical(id="app_recent_runs") {
    Label("Recent Runs")
    ListView(id="recent_runs_list")
}
```
