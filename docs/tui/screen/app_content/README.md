# App Content Container

The main content area of the application, which switches between different primary views.

## Structure

```text
Container(id="app_content") {
    # One of the following views:
    CompanySearchView
    CompanyDetail
    PersonList
    ApplicationView
}
```
