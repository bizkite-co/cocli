# Abstract out the lint-test-hash-bypass to a tool

Make that lint-test-hash linter and test runner bypasser as much of a portable tool as possible. I want to use it in other repos that aren't Python, and which might be in Windows PowerShell environments
