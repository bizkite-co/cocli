# Compute Resources

This is where we should specify, configure, and monitor the compute resources for this campaign, such as Raspberry PI instances, the Docker containers on them, or Fargate workers, etc.

