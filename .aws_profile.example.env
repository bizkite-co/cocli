# Cocli AWS Profile Override
# Copy this to .aws_profile.env and it will be loaded by .envrc
# The 'cocli campaign set' command manages this file automatically.

export AWS_PROFILE="aws-profile-name"
